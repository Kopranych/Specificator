/*
 * Create_spec.h
 *
 *  Created on: 7 мая 2016 г.
 *      Author: Копраныч
 */

#ifndef CREATE_SPEC_H_
#define CREATE_SPEC_H_



#endif /* CREATE_SPEC_H_ */


#include <stdio.h>
#include <stdbool.h>

typedef struct{         //Cоздали новый тип данных с именем device представленный ввиде структуры
	char element[20];   //которая имеет имя элемента
	int number;			//и порядковый номер
}device;

int device_spec(char *name);//функция которая создает в файле на диске спецификацию для внесения в нее элементов
int complect(char *name);   //функция отображающая данные спецификации хранящиеся в файле
